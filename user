FROM alpine:latest
RUN addgroup -S fiplan && adduser -S -g fiplan preti
WORKDIR /app
COPY . /app
RUN <<EOF
apk update
apk add python3
EOF
USER user@DESKTOP-HBJ2RAI
ENTRYPOINT [“python3”, “programa.py”]
